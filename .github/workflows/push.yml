name: '🔀️ Push'

on:
  push:
    branches:
      - main
      # prerelease branches
      - canary
      - develop
      - ci/**
      - feature/**
      - refactor/**
      - release/**

concurrency:
  group: push-${{ github.ref }}-1
  cancel-in-progress: true

jobs:
  call-init:
    name: '🎉️  Init'
    if: contains(github.event.head_commit.message , '[build]') || contains(github.event.head_commit.message , '[b]') || github.ref == 'refs/heads/main'
    uses: JeromeFitz/jeromefitzgerald.com/.github/workflows/_init.yml@ci/github-action-refactor

  call-lint:
    name: '🚨️  Lint'
    if: contains(github.event.head_commit.message , '[build]') || contains(github.event.head_commit.message , '[b]') || github.ref == 'refs/heads/main'
    needs: call-init
    uses: JeromeFitz/jeromefitzgerald.com/.github/workflows/_lint.yml@ci/github-action-refactor

  call-release:
    name: '🏷️  Release'
    if: contains(github.event.head_commit.message , '[build]') || contains(github.event.head_commit.message , '[b]') || github.ref == 'refs/heads/main'
    needs: call-lint
    uses: JeromeFitz/jeromefitzgerald.com/.github/workflows/_release.yml@ci/github-action-refactor

  # call-deploy:
  #   name: '🚀️  Deploy'
  #   if: github.event.pull_request.draft == false
  #   needs: call-lint
  #   uses: JeromeFitz/jeromefitzgerald.com/.github/workflows/_deploy.yml@ci/github-action-refactor
  #   with:
  #     GITHUB_TOKEN: ${{ secrets.GH_BOT_TOKEN }}
  #     VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  #     VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  #     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  #     CREATE_COMMENT: false
  #     DELETE_EXISTING_COMMENT: false
  #     PR_LABELS: false
  #     PRODUCTION: false

  deploy:
    name: '🚀️  Deploy'
    if: github.ref == 'refs/heads/main'
    needs: call-lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GITHUB_TOKEN: ${{ secrets.GH_BOT_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
    steps:
      - name: '🐙️  Checkout'
        id: push-checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: '🔺️ Deploy'
        id: push-deploy
        uses: BetaHuhn/deploy-to-vercel-action@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GH_BOT_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          CREATE_COMMENT: false
          DELETE_EXISTING_COMMENT: true
          PR_LABELS: false
          PRODUCTION: true
