# name: 'Test'
name: "🧪  "
description: "Localized Test"
author: "JeromeFitz"

inputs:
  TURBO_TEAM:
    description: "Provide Team ID for Turbo"
    required: true
    type: string
  TURBO_TOKEN:
    description: "Provide Token for Turbo"
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: "🧪  Test"
      id: test
      shell: bash
      run: pnpm turbo run test:unit:coverage --filter='!@jeromefitz/ds' --cache-dir=".cache-turbo"
