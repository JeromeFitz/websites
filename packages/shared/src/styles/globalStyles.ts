/**
 * @note(styles) template for individual sites
 */
const globalCss = {}

const globalRoot = {}

const globalStyles = {
  ':root': {
    ...globalRoot,
  },
  ...globalCss,
}

export { globalStyles }
