import type { Variants } from './Button.types.js'

const VARIANTS: Variants = {
  DEFAULT: 'default',
  EMPTY: 'empty',
  GHOST: 'ghost',
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
  TERTIARY: 'tertiary',
  TEXT: 'text',
}

export { VARIANTS }
