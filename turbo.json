{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["pnpm-workspace.yaml"],
  "pipeline": {
    "//#format": {
      "dependsOn": [],
      "inputs": ["*.cjs", "*.js", "*.json", "*.md", "*.mjs", "*.ts"]
    },
    "build": {
      "outputs": [".cache/**", ".next/**"],
      "dependsOn": ["^build"],
      "inputs": ["src/**", "*.cjs", "*.js", "*.mjs", "package.json", "vercel.json"],
      "env": ["VERCEL_ENV"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false
    },
    "format": {
      "dependsOn": ["^//#format"],
      "cache": false
    },
    "lhci": {
      "cache": false
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": ["src/**", "*.cjs", "*.js", "*.mjs", "package.json", "vercel.json"]
    },
    "semantic-release:mono": {
      "cache": false,
      "dependsOn": ["lint"]
    },
    "start": {
      "cache": false
    },
    "test": {
      "outputs": ["coverage/**"],
      "dependsOn": ["lint"],
      "inputs": ["src/**", "*.cjs", "*.js", "*.mjs", "package.json", "vercel.json"]
    }
  }
}
